$('#alert').replaceWith("<%= j render(partial: 'layouts/shared/alert_box') %>");
setTimeout(function() {
  $(".alert-auto-dissapear").fadeOut(1500);
},2000);
var contactId = '<%= @contact.id %>'
var invitedText = '<%= sweet_full_date_time(@contact.invited_at) %>'
$('td.invited_at', 'tr#contact_' + contactId).text(invitedText);
$('a.mark-invited', 'tr#contact_' + contactId).remove();
