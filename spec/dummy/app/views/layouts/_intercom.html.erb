<% if Rails.env.production? %>
  <script>
    window.intercomSettings = {
      <% if current_spree_user %>
        app_id: "wmrzionr",
        name: "<%= current_spree_user.name %>", // Full name
        email: "<%= current_spree_user.email %>", // Email address
        created_at: <%= current_spree_user.created_at.to_i %> // Signup date as a Unix timestamp
      <% else %>
        app_id: "wmrzionr"
      <% end %>
    };
    </script>
  <script>(function(){var w=window;var ic=w.Intercom;if(typeof ic==="function"){ic('reattach_activator');ic('update',intercomSettings);}else{var d=document;var i=function(){i.c(arguments)};i.q=[];i.c=function(args){i.q.push(args)};w.Intercom=i;function l(){var s=d.createElement('script');s.type='text/javascript';s.async=true;s.src='https://widget.intercom.io/widget/wmrzionr';var x=d.getElementsByTagName('script')[0];x.parentNode.insertBefore(s,x);}if(w.attachEvent){w.attachEvent('onload',l);}else{w.addEventListener('load',l,false);}}})()</script>
<% end %>
